Write PL/SQL Procedure Using Functions:
PL/SQL procedures can use functions to perform specific tasks.

CREATE OR REPLACE FUNCTION calculate_bonus(salary NUMBER) RETURN NUMBER IS
    bonus NUMBER;
BEGIN
    IF salary > 50000 THEN
        bonus := salary * 0.1;
    ELSE
        bonus := salary * 0.05;
    END IF;
    RETURN bonus;
END;
