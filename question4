Execute DCL and TCL Commands:
DCL (Data Control Language) and TCL (Transaction Control Language) commands manage access and transactions.

-- DCL
GRANT SELECT ON employees TO user1;

-- TCL
BEGIN
    UPDATE orders SET status = 'Shipped' WHERE order_id = 1001;
    COMMIT;
END;
